# Disk Management

Lets say we have `node1` and `node2` initially with no disks, we will add one disk to each using the first node's UI, then restart `node2`.

1. From the UI a user adds a disk to `node1` 

